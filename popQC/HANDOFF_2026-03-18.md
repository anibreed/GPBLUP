# popQC Work Handoff (2026-03-18)

## Completed Today
1. HWE skip behavior implemented and verified
- Condition: HWE_THRESHOLD == 0.00
- Behavior: HWE test and HWE-based filtering are skipped
- Verified by runtime logs showing HWE skip path

2. Parent-seek logic updates implemented
- Added parameter: PARENT_SEEK_NSNPS (default 500)
- Added parameter: PARENT_SEEK_G_TOL (default 0.15)
- Enabled G cutoff filtering as actual candidate filter: |G - 0.5| <= tolerance
- Parent-seek SNP selection is handled in dedicated subroutine with filters:
  - autosome only (Chr 1-18)
  - MAF >= 0.30
  - call rate >= 0.99
  - conditional HWE p >= 0.001 (when HWE enabled)
- Within-chromosome selection updated to MAP Pos-based binning (bp-range bins)

3. Step 7 runtime validation performed
- action=1 generation was validated
- parent assignment path also validated in test scenario
- Observed assignment example in test run:
  - Animal: DD2433143
  - Outcome: MULTI_RESOLVED
  - NewSire: DD2531313
  - NewDam: DD2432639
  - Animals with parent assigned (B/C): 1

## Pending for Next Session
1. Tune thresholds for production-like strict criteria (requested to do later)
- Candidate compatibility threshold (PARENT_SEEK)
- G tolerance (PARENT_SEEK_G_TOL)
- Sex/Mendel consistency settings

2. Re-run on target data with final thresholds and confirm stable assignment quality

## Code Areas Updated
- /home/dhlee/GPBLUP/popQC/popQC.f90
- /home/dhlee/GPBLUP/src/M_Variables.f90
- /home/dhlee/GPBLUP/src/M_readpar.f90

## Notes
- Temporary test artifacts were removed on request to avoid confusion.
- This handoff file is kept as the single continuation reference for tomorrow.
